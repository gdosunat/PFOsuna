import { ControlErroresPipe } from './control-errores.pipe';

describe('ControlErroresPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlErroresPipe();
    expect(pipe).toBeTruthy();
  });
});
